import React from 'react';

/**
 * Renders a component <string> &lt;NotFound/> </strong>.
 * It is a fallback component in case no routes are matched.
 *
 */
const Notfound = () => <h1>Not found</h1>

export default Notfound
